問題: 會怎麼寫把GitHub 用戶名對應到具體對象 ---> AI用盒子的比喻告訴我ref會存放fetch依照名字抓來的資料。

問題: 為什麼在未找到人的情況看F12會沒有資料盒的HTML->AI以開關的比喻說明v-if的作用，在資料盒為空時v-if會使內部的東西消失。

問題: 如何理解v-if 跟 v-for ---> AI比喻v-if像開關，v-for像生產線，v-for裡的東西會重複好幾次，像是會把專案列表盒裡的每個專案，一個一個拿出來顯示。

問題: 如何理解template ---> AI回答template像是一個不會出現在畫面上的包裝盒，用來把東西包在一起template  跟div差別在於template是透明的不會顯示。

問題: 把深色 class 加到 body 上面起到了什麼作用 ---> 想像有一個大房間（body）和很多小盒子（div、input、.repo-card）。沒有深色 class 的時候每個小盒子都要一個一個改，很麻煩，有深色 class 在 body 上的時候整個大房間都改了。